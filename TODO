towards some form of iso compliance:
    https://www.complang.tuwien.ac.at/ulrich/iso-prolog/prologue
    - `"hello"` is a native STRING type, not a list of char codes (should throw type_error(evaluable, '.'/2) not type_error(evaluable, "hello"))

ease of use on the default linux target:
    [] readline/history in repl

freestanding:
    - better bsp
        - get rid of the unity build
    - dont rely on macros at all for platform specifics
        - there should be an io stub for flush

wasm demo:
    - add a site deployment
    - ledit should use core.pl for internals
    - some simpler non distracting style
    - a workable terminal
    [x] some real constraints, arm m0+ (32kb sram profile: 16kb term pool, 4kb strings, 128 clauses, 512 bindings)
    [x] it should have some usage stats, and constrained to m0 things (resource bars: heap, strings, clauses, bindings, stack)

testing:
    - move quad to a pl file, separate TAP and jUnit outputs