towards some form of iso compliance:
    https://www.complang.tuwien.ac.at/ulrich/iso-prolog/prologue
    [x] `"hello"` is a native STRING type, not a list of char codes (should throw type_error(evaluable, '.'/2) not type_error(evaluable, "hello"))

ease of use on the default linux target:
    [] readline/history in repl

wasm demo:
    - add a site deployment
    [x] ledit should use core.pl for internals (removed l_member/2 and l_reverse/3, uses core.pl's member/2 and reverse/3)
    - some simpler non distracting style
    - a workable terminal
    [x] some real constraints, arm m0+ (rp2040 profile: 64kb term pool, 8kb strings, 256 clauses, 1024 bindings)
    [x] it should have some usage stats, and constrained to m0 things (resource bars: heap, strings, clauses, bindings, stack)

testing:
    - move quad to a pl file, separate TAP and jUnit outputs

ledit:
    - printing 20 lines will need 100k termpool, for what?